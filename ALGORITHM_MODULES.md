# 通用视觉算法工程族（分批实施）

目标：将标签检测中的可复用视觉接口及算法下沉到DP.Vision工程族，按能力类型组织契约，按基座提供可替换实现。不是把标签业务搬进通用底座，也不是让DP.Vision核心引用所有原生库。

首批已新增 `DP.Vision.Algorithms` 和 `DP.Vision.OpenCv`，真实迁入单字比较、固定/空白检测并接回标签工作台。范围、验证和未完成项见 [ALGORITHM_MIGRATION.md](ALGORITHM_MIGRATION.md)。其余扩展仍是方案，不能按表中的拟议项宣称已实现。

## 1. 两个独立维度

- **能力类型**：形态学、分割、文字读取、字符外观、匹配、条码读取、条码/QR质量等。类型定义调用方使用的中立契约。
- **算法基座**：OpenCV、HALCON、ONNX Runtime、ZXing等。基座提供其中部分契约的实现，不承诺每个基座都支持所有能力。

先按实际需求划分命名空间和模块，不为每一个算子建立独立DLL，也不提前建立所有“基座×类型”的空项目。

## 2. 推荐的初始工程划分

| 工程 | 职责 | 依赖原则 |
|---|---|---|
| `DP.Vision`（已有） | 图像租约、像素格式、帧身份、原图几何及现有通用显示契约 | 不引用算法实现、标签业务或厂商SDK |
| `DP.Vision.Algorithms`（首批已实现） | 中立算法接口、输入输出、执行状态、能力与参数契约 | 引用DP.Vision；不引用厂商和UI |
| `DP.Vision.OpenCv`（首批已实现） | OpenCV实现及其图像/几何适配 | 引用算法契约和OpenCV，不引用标签业务 |
| `DP.Vision.Halcon`（拟议） | HALCON实现及其适配 | 引用算法契约和HALCON；运行库、平台与许可证要求限定在此侧 |
| `DP.Vision.Onnx`（拟议） | 适用的识别、分割或质量模型实现 | 引用算法契约和推理运行库；模型及能力分别声明 |
| `DP.Vision.Zxing`（拟议） | 适用码制的读取实现 | 引用算法契约和ZXing；不冒充印刷质检实现 |
| `DP.Vision.Winform` / `DP.Vision.WPF`（已有） | 原生显示和ROI交互 | 不因新增算法而被迫引用OpenCV、HALCON或标签业务 |

初期在 `DP.Vision.Algorithms` 内按 `Morphology`、`Segmentation`、`Text`、`Matching`、`Barcodes` 等命名空间组织。随着独立发布及依赖需求实际出现，可拆成 `DP.Vision.Morphology`、`DP.Vision.Text`、`DP.Vision.Barcodes` 等能力工程；对应实现可进一步拆成 `DP.Vision.OpenCv.Morphology`、`DP.Vision.Halcon.Barcodes` 等。能力包不反向引用基座包。

依赖方向固定为：实现工程引用中立契约，契约引用核心。标签流程引用中立契约；宿主装配时才选择具体实现。

## 3. 哪些能力下沉

| 能力 | DP.Vision侧 | 留在DP.LabelInspection侧 |
|---|---|---|
| 形态学与区域处理 | 阈值、区域集合、连通域、形态学等通用处理 | 本ROI是否应运行这些处理 |
| 字符分割 | 实际字符字块、几何、分割完整性及必要输入 | 标签配方启用规则、制库人工审核与发布流程 |
| OCR | 实际读数、可靠性、可用位置证据；不同实现特有证据可选 | 引导值来源、相等比较及不一致后停止本ROI质检 |
| 字形配对与外观 | 通用字块身份/参考对应、字形差异、文字外观质量算法 | 业务字库类别、固定修订选择、任务绑定及人工替换审批 |
| 条码/QR读取 | 码制、内容、位置和可提供的结构证据 | 业务字段绑定和引导值判断 |
| 条码/QR质量 | 独立质量算法、质量指标、局部异常及阻断原因 | 是否选中质量项目、ROI综合NG和下一ROI调度 |
| 固定/空白质量 | 通用参考区域比较、空白异常检测 | 固定/空白ROI类型的业务约束及检查项目默认值 |
| 定位与匹配 | 通用定位结果、变换、匹配指标及适用范围 | 采用哪种坐标方案及如何关联标签配方 |

文字完整质检可以作为DP.Vision侧的组合算法，内部组合分割、配对和比较；它不能接收整个标签配方或自己遍历标签ROI。业务侧负责提供已经解析好的参考快照及算法参数。

字形参考像素、标识和必要元数据可形成中立输入，但不让视觉算法管理标签字库的数据库、发布、审核或修订升级。

## 4. 中立不等于丢失语义

- 公开契约不用Mat、HObject、ONNX张量或UI控件；厂商句柄仅在实现侧管理。
- 复用统一 `IImageSource`、`ImageInfo`、几何及坐标约定，不复制一套与底座冲突的图像模型。`ImageBuffer`已转为内部存储实现；此次公开类型变更要求宿主重新编译。标签ImageFrame保留为不可变业务快照，通过显式复制适配接入。
- 共享输入是只读租约。算法工作图单独拥有；返回字块/证据必须在调用结束后仍然有效，释放责任明确。
- 形态学必须规定前景含义、结构元素、原图像素单位和边缘处理；匹配与质量必须说明分数含义、阈值和覆盖限制。不能仅靠相同方法名宣称跨基座结果等价。
- 分开执行状态、质量结论、质量指标和缺陷明细；缺能力或未完成由标签流程映射成本ROI NG，不把通用契约绑定到标签专用Verdict或Finding类型。
- 已有OCR可按证据契约复用，但必须核对帧身份、ROI/坐标、配置及适用性；不能把CTC位置当真实物理分割。
- 不同基座支持的格式、位深、几何和模型明确声明；不支持时明确返回原因，不静默降精度或更换实现。

## 5. 避免中立接口带来大量复制

接口隔离不意味着每一个低层算子调用都必须把原生图像转换回中立图像。

对外暴露有实际替换价值的分割、完整质检等模块；实现内部可持有原生工作图并执行多个算子。跨模块或跨基座的转换通过明确的适配发生，记录所有权与必要复制。当前IImageSource只提供复制式字节读取，不向外部实现公开任意可写内存别名，不能直接宣称全链零拷贝。详见[统一图像源](UNIFIED_IMAGE_SOURCE.md)。

不能为了消除转换而把原生句柄塞入object，再要求调用方猜测实际类型。性能优化应在契约确定后测量，而不是先牺牲契约安全。

## 6. 标签侧的最终职责

DP.LabelInspection保留：ROI类型与质量/数据项目选择、各ROI前置需求、引导值比较、当前ROI提前NG、继续后续ROI、ROI证据父子汇总和任务结果输出。

宿主显式装配所选视觉实现。缺实现、缺模型、缺参考或不支持当前配置时，不自动换基座；按用户已确认的规则记本ROI NG并继续其他ROI。

## 7. 逐步迁移，不一次性搬空

1. 从现有字符分割、单字比较和读码契约提炼中立输入输出及状态，保留旧接口适配。
2. 将二维码质检从条码实现内部硬编码创建改为独立实现选择；OpenCV现有算法先接入，不能因为计划支持HALCON而宣称已有对应实现。
3. 抽离文字读取/内容业务判断/文字质量组合，按ROI驱动规则装配。
4. 将已验证的通用视觉实现迁入对应DP.Vision基座工程，再逐项替换标签侧调用。
5. 在有真实算法和验证用例时增加HALCON或深度替代实现；不按完整矩阵创建空壳。

验收包括：核心与契约没有厂商/业务反向依赖；旧公开入口仍可使用；替换算法不改变ROI流程；数据失败不调用质量算法；本ROI NG不漏检下一ROI；原图/坐标/证据生命周期正确；不同算法的指标差异有记录。通用显示回归、标签业务回归和原生适配验证分别执行，不能互相替代。
