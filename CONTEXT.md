# 通用视觉画布

描述图像、检测范围和视觉证据的共同语言，不包含标签内容或厂商算法语义。

## Language

**图像源（Image Source）**：一张确定且不可变图像的可读表示，可供显示和处理共享。它不是相机设备、图像序列或当前窗口截图。
_Avoid_：可写帧槽位、预览队列

**图像采集 Provider（Image Acquisition Provider）**：独立管理某类厂商SDK、设备发现、连接、配置、采集和关闭的硬件适配模块；它向公共层返回中立图像租约，不包含工作流节点语义。
_Avoid_：工作流视觉插件、全局图像仓、节点内部SDK对象

**厂商原生运行时（Vendor Native Runtime）**：某个Provider的托管程序集之外、必须由进程在运行期解析到的厂商本地库。托管程序集在编译期存在，不等于原生运行时已部署；两者缺失的时机不同（编译期 vs 运行期），但都必须让采集节点在首节点执行前失败，而不是等到采集时抛原生异常。
_Avoid_：编译开关、许可证有效性、设备在线

**逻辑采集源（Logical Acquisition Source）**：流程和业务使用的稳定SourceId，由机器配置绑定到一个Provider设备配置；更换Provider不要求修改流程文档。
_Avoid_：HALCON接口字符串、设备数组下标、厂商SDK句柄

**物理资源键（Physical Resource Key）**：跨逻辑SourceId识别同一实际设备的稳定键，用于占用、互斥和诊断；它不作为流程节点的业务配置。
_Avoid_：NodeId、FrameId、仅在单次枚举中有效的设备序号

**设备租约（Device Lease）**：采集运行时针对一个物理资源授予某次操作或根运行的有限使用权；释放设备租约不释放已交给运行的图像，释放图像租约也不表示设备已关闭。
_Avoid_：图像Retain、服务实例存在、准备检查成功

**显示图块（Display Tile）**：某个采样级别固定网格中的局部图像表示，边缘可小于常规尺寸。它不是任意检测ROI，也不意味着字符或物体的分割结果。
_Avoid_：字符切割、检测区域

**图像帧（Image Frame）**：具有确定像素内容、格式和身份的一次图像观测。相同图像的不同显示视图不构成新图像帧。
_Avoid_：窗口截图、当前画面

**检测范围（ROI）**：用于表达算法处理范围的配置，可具有包含或排除用途。它不是对该范围检测成功与否的证据。
_Avoid_：缺陷框、检测结果

**像素区域（Region）**：原图像素集合，可包含孔洞和多个互不相连的部分。一个区域不等于一个连通部分或一个外接矩形。
_Avoid_：包围框、单个轮廓

**亚像素轮廓（Contour）**：保留顺序的连续坐标点列，具有明确的开放或闭合状态。闭合轮廓不自动表示内部填充区域。
_Avoid_：所有XLD对象、栅格掩码

**图层（Layer）**：具有独立显示顺序、可见性和样式的视觉内容集合。图层的开关不改变检测证据。
_Avoid_：算法任务、图像通道

**视图（Vision View）**：一张底图及严格属于该图坐标系的图层集合。原图、二值图、局部图分别作为视图，不隐式混合局部坐标与原图坐标，也不表达流程节点或执行状态。
_Avoid_：流程节点、随意叠图、产品判定

**视图集合（View Collection）**：外层选择交给视觉界面浏览的一组有序视图，其成员不暗示流程结构或处理先后。预览未保留只表示显示资源不可用，不代表正式检测结果丢失。
_Avoid_：流程运行、任务队列、正式报告

**显示LOD（Display LOD）**：依照当前观察尺度选择的轮廓显示细节。它不是修改后的测量轮廓，也不是几何检测公差。
_Avoid_：精度优化、原始点压缩

**原图坐标（Original Image Coordinates）**：相对于同一张原图的连续坐标，独立于窗口缩放和平移。局部裁图坐标不能在未转换原点时当作原图坐标。
_Avoid_：屏幕坐标、ROI局部坐标

**预览帧（Preview Frame）**：供操作者观察的图像及其对应叠加内容。未显示某一预览帧，不代表允许漏检相应生产图像。
_Avoid_：检测任务、已完成检测

**ROI配置文档（ROI Document）**：算法范围的不可变已提交快照，包含ROI身份、几何、用途、启用状态及编辑约束。它不是当前拖动预览，也不依附某一份检测结果。
_Avoid_：叠加层、显示路径、缺陷列表

**编辑事务（Edit Gesture）**：从开始操作到一次提交或取消的配置变更。移动过程仅更新预览，一次拖动最多产生一条历史记录；取消不改文档。编辑期间暂缓取最新预览，不暂停或丢弃检测任务。
_Avoid_：每次鼠标移动保存一次、修改原始证据

**几何编辑约束（ROI Constraint）**：明确保留的形状不变量，如圆等半径、矩形轴对齐。约束经构造验证并持久化，不是工具栏名称推断。
_Avoid_：视觉上大概像圆、自动纠正原图
